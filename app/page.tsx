// Root path — next-intl middleware redirects this to /es/ or /en/
// This file exists only to satisfy Next.js; it should never be rendered directly.
export default function RootPage() {
  return null;
}
